<h1> Note Details </h1>

<h2>Title: <%= @note.title %></h2><br>
Body Text: <%= @note.body %><br>
<%= @note.created_at %><br>
<%= current_user.name %><br>

<h3><%= link_to "Edit Note", "/users/#{current_user.id}/note/#{@note.id}/edit", method: :get %></h3>
<h3><%= button_to "Delete Note", "/users/#{current_user.id}/note/#{@note.id}", method: :delete %></h3>